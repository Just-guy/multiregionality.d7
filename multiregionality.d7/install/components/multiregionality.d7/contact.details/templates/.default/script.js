(function (window) {
	'use strict';

	window.JCMultiregionalityContactDetails = function (arParams) {
		//debugger
		this.result = arParams;
		BX.ready(BX.delegate(this.init, this));
	}

	window.JCMultiregionalityContactDetails.prototype = {
		init: function () {

		},
	}
})(window)
